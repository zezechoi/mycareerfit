# Career FIT OS — 의사결정 기록 (DECISIONS)

> 중요한 선택과 그 이유. "내가 왜 이렇게 했더라?" 싶을 때 보는 용도.
> 큰 결정이 생길 때마다 맨 위에 새로 추가 (최신순).
> 지난 결정은 지우지 않는다 — 생각이 바뀐 흐름 자체가 기록이다.

작성 형식:
```
## [날짜] 결정 제목
- 결정: 무엇을 정했는가
- 이유: 왜 그렇게 했는가
- 대안: 고려했지만 안 택한 것 (있으면)
```

---

## [2026-06] JD 입력: 다중 이미지 = 한 공고로 합쳐 분석

- **결정:** JD 이미지를 여러 장(최대 6장) 누적 업로드(파일 다중선택·드롭·Ctrl+V), 올린 순서대로 한 메시지에 함께 보내 "하나의 공고"로 합쳐 분석한다(1장씩 따로 X).
- **이유:** 잡코리아 등 긴 공고는 캡처 1장에 안 담김 → 나눠 올려야 전체가 들어감.
- **상한 6장:** 토큰·비용·PARSE_FAIL 폭증 방지. 초과 시 안내. (각자 키·자기 비용이라도 합리적 상한은 유지.)
- **순서 유지:** 공고는 위→아래 흐름이 있어 올린 순서 그대로 전달. 수동 재정렬 UI는 안 만듦(필요시 제거 후 재추가). 단일 이미지·텍스트 입력은 그대로.

## [2026-06] 못 읽는 링크는 막지 말고 우회 안내

- **결정:** 노션·피그마·구글독스처럼 외부에서 텍스트 추출이 안 되는 링크는, 기술 에러("리다이렉트가 너무 많아요" 등)를 노출하지 않고 사람 친화 안내로 대체하고 텍스트/파일 입력으로 유도한다. 빈/너무 짧은 응답(로그인 stub)도 실패로 보고 빈 분석을 막는다.
- **이유:** 기술 에러는 "이게 뭐지?" 혼란만 줌. JS 렌더링·로그인·권한 때문에 외부 추출이 구조적으로 안 되는 링크가 많아, 막다른 길 대신 바로 다른 방법(텍스트/파일)으로 안내하는 게 실사용에 맞음.
- **세부:** 실패 시 탭 자동 전환은 안 함 — 전환하면 링크 화면의 안내가 가려지므로, 안내를 남기고 "어느 탭으로 가라"만 알려줌.

## [2026-06] 지원 후 관리 = 앱 내장 X, 엑셀 내보내기로

- **결정:** 지원 후 관리(마감일·진행추적·메모 등)는 앱 안에 만들지 않는다. 대신 거른 결과(회사명·직무·FIT점수·판정·어필포인트 등)를 엑셀로 내보내, 사용자가 받아서 직접 관리한다.
- **경계선:** 플랫폼 = "거르기"까지(지원 전). 엑셀 = "거른 다음부터"(지원 후). 역할 분리.
- **이유:**
  - 관리 기능을 앱에 다 넣으면 무거워지고 본질(지원 전 거르개)을 벗어남 → 덕지덕지化.
  - "쓸 이유 없어지지 않나?" 우려 → 안 생김. 거르는 과정은 플랫폼에서만 가능(내 기준이 분석·어필·판정을 관통). 엑셀은 그 산출물이지 플랫폼 대체가 아님.
  - 사용자의 "한 곳에서 관리하고 싶다" 욕구는 "데이터 들고 나가게" 해서 가볍게 충족.
  - 자기 검증: 제제 본인도 엑셀로 따로 관리 중 → 다운받아 정리하는 게 실제로 편함.
- **대안(기각):** 앱 내 CRM 풀세트(마감일·필터·하이라이트·메모) → 무겁고 본질 벗어남.
- **검증 예정:** 다음 피드백에서 "엑셀로 받아 직접 관리 vs 앱 내 관리, 뭐가 나아?" 확인.

## [2026-06] 이력서 초안 → 자기소개서 초안으로 정체성 정리

- **결정:** "이력서 초안" 기능을 "자기소개서 초안"으로 변경. 이력서(학력·경력 항목)는 사용자가 이미 갖고 있어 AI가 도울 영역이 아님. AI는 자소서(지원동기·자기소개)를 돕는다.
- **이유:** 이력서/자소서가 모호하게 섞여 있었음. 이력서는 사실 정보라 AI가 지어낼 위험만 크고, 정작 막막한 건 자소서. 도울 수 있는 데 집중.
- **추가 결정(자소서 품질):** "구체적이면 환각 위험 / 안전하면 식상"의 트레이드오프가 있음. 거짓이 치명적이므로 **"안전하지만 식상"을 선택**. 구체화(브랜드명·수치)는 사용자 몫으로 안내 — AI는 막막함을 없애는 출발점, 완성은 사용자가.
- **반영:** 핵심 경험을 개조식→서술형 문단으로, 화살표·특수기호 제거, 환각 금지 강화(프로필에 없는 숫자·기간·시차·고유명사 생성 차단 — 5년·8시간 시차·Lancôme 사례), "AI가 만든 초안이니 본인 경험·말투로 다듬어 쓰라" 안내 한 줄 추가.

## [2026-06] 각자 키 방식으로 확정 전환 (스폰지클럽 확장)

- **결정:** "내 키 공유 + 비번" → **"각자 API 키 입력(키만 게이트)"**으로 전환 확정.
- **이유:** 검증(5명) 완료. AI에 익숙한 스폰지클럽에 먼저 확장. 자기 키·자기 비용이라 내 비용 0. 원래 기획(하네스 #1 "각자 키, 각자 비용")으로 복귀.
- **구독/결제(C안):** "돈 낼 의향" 신호 확인 후. 지금은 아님.
- **위치:** 기존 "[2026-05] 확장 시 비용 구조 — 각자 API 키 발급 방향" 메모의 **A안이 실제 결정으로 확정**된 것. 구현 상세는 아래 "[2026-05] 각자 키 전환 — 구현 기록" 참고.

## [2026-05] 각자 키 전환 — 구현 기록 (무중단 4단계)

> 결정 배경·확정은 위 [2026-06] "각자 키 방식으로 확정 전환" 참고. 여기는 어떻게 구현했는지 기록.

- **방식:** 사용자가 `sk-ant-...` 키 입력 → localStorage(`careerfit_apikey`, 브라우저에만) → `x-anthropic-key` 헤더 → 프록시(`/api/claude`)가 그 키로 Anthropic 호출. 오너 env 키(`ANTHROPIC_API_KEY`)·비번(`APP_PASSWORD`) 경로 완전 제거(키가 곧 게이트).
- **단계적 전환(무중단):** ① 서버 3종이 키 헤더 수용(비번 폴백 유지) → ② 프론트를 키 입력으로 → ③ 에러 UX(입력 시점 키 검증 + 분석 시점: 401/403 키삭제·재입력 / 429·크레딧은 키 유지) → ④ 비번 폴백 제거(키 필수)+문서. 각 단계 배포돼도 라이브 유지.
- **입력 시점 검증:** 키 저장 시 그 키로 최소 토큰(`max_tokens:1`) 테스트 호출 → valid(200/429/크레딧)만 통과, invalid(401/403) 차단·재입력, unknown(네트워크·5xx) 차단·재시도 안내. "일단 통과 후 나중에 막기"를 "처음부터 유효한 키만 통과"로.
- **보안:** 키는 본인 브라우저 평문 저장(자기 키라 허용, 공용 PC 경고 문구). 프록시·검증 호출 모두 키 미로깅, 에러 메시지에 키 미노출. `fetch.js`는 Anthropic 미호출이라 키 형식(`sk-ant-`)만 게이트 + SSRF 보호 유지(유효성 검증은 비용 들어 생략).
- **마이그레이션:** 옛 `careerfit_password` 슬롯은 로드 시 폐기, 키 형식 아닌 저장값은 무시 → 기존 사용자는 다음 접속 때 본인 키 입력 유도.
- **데이터 분리:** `CLOUD_SYNC=false` 그대로(브라우저별 분리).
- **후속:** 오너가 Vercel에서 `APP_PASSWORD`·`ANTHROPIC_API_KEY` 환경변수 삭제 가능(앱 동작 영향 없음).

## [2026-05-27] 최소 멀티유저 구현 — 기준 개인화 방식

- **결정:** "풀 멀티유저"가 아니라 딱 "사용자가 자기 기준으로 자기 직무 공고를 판정받는 것"까지만 구현. 4단계로 나눠 각 단계를 작동 상태로 커밋.
  1. CONFIG·프롬프트에서 제작자/K-뷰티 하드코딩 제거 → 범용 폴백 템플릿으로 (`28e3a18`)
  2. 온보딩이 이력서에서 본인 기준 추출(goal·3축 def/basis·어필 우선순위) → 프로필 저장, 결과 화면에서 수정 가능 (`b62cd7b`)
  3. JD 분석이 프로필 기준 우선 적용(폴백 CONFIG), 프로필 goal 주입 (`5ea5626`)
  4. 필수 요건 필터 산업 중립화: `cosmetics` → `requirement{level:mismatch|ok}` ([내 프로필] 대비 판정) (`2069d74`)
- **핵심 설계:** 3축 **key(job/env/growth)·라벨·비중은 고정**, **의미(def/basis)만 개인화**. 키를 고정해야 판정·보드·점수 로직이 안정적이고, 의미만 바꿔도 사용자별 판정이 된다.
- **확정한 선택지:**
  - ① 제작자(최지예)의 기존 프로필은 개인화 기준이 없으므로 **재온보딩 전까지 범용 기준으로 폴백**. 마이그레이션 코드 없이 폴백으로 흡수.
  - ② 추출된 기준은 **온보딩 결과 화면에서 직접 수정 가능**(goal·3축 def·어필 우선순위 전부 editable).
- **건드리지 않은 것:** 데이터 분리/동기화는 기존 localStorage 방식 그대로(브라우저별 분리, CLOUD_SYNC=false). 결제·권한관리 등 과한 기능 안 만듦.
- **레거시 호환:** 기존 히스토리의 `cosmetics` 결과는 표시 단계에서 `mismatch/ok`로 매핑해 그대로 보여줌(`required`→경고).
- **대안:** SYSTEM_PROMPT를 프로필별 함수로 만드는 안 → 기각. 시스템 프롬프트는 범용 역할 정의만 두고, 개인 컨텍스트(프로필·goal)는 분석 시점 user 메시지(`buildProfileText`)로 주입하는 게 더 단순·안정적.

## [2026-05] 차별점 + 타깃 정의 (경쟁 리서치 기반)

### 차별점 — 시장의 다른 도구와 무엇이 다른가
경쟁 리서치 결과, 원티드·리멤버·사람인·AI Resume Screener 등 시장의 거의 모든
도구가 **"기업이 사람을 고르는"** 방향(JD 보고 인재 추천, 합격 확률 예측)이었음.
Career FIT OS는 정반대 — **"내가 회사를 고르는"** 도구.

핵심 차별점 3가지:
1. **선택하는 도구** (vs 선택받는 도구) — 합격 확률이 아니라 "나랑 맞는가" 적합도 판단. 시장 전체와 관점이 반대라 모방이 어렵다.
2. **플랫폼 독립** — 원티드는 원티드 공고만, 사람인은 사람인 공고만 본다. 우리는 이미지/텍스트로 **어떤 플랫폼의 공고든** 한 기준으로 판단. 플랫폼 사업자는 자기 공고를 밀어야 해서 구조적으로 못 하는 영역.
3. **내 기준 설정** — 합격 확률은 플랫폼 알고리즘이 정하지만, 우리는 사용자가 직무·환경·성장 기준을 직접 세팅.

> 주의: "예상 면접 질문"은 원티드에 이미 있음(AI 피드백까지). 데이터 양에서 못 이기는 싸움 → 차별점이 아니므로 우선순위 낮춤.

> 정직한 한계: JD만으로 회사 분위기를 100% 알 수는 없음. "정확한 예측"이 아니라 "지원 전 거르개(업무+분위기 방향성 판단)"로 포지셔닝.

### 타깃 — 누구를 위한 도구인가
**"경력은 있는데, 다음 자리는 신중하게 고르고 싶은 사람."**
이직자 · 중고신입 · 공백기 후 복귀자. 공통 심리 = "또 잘못 가면 안 된다."
(신입의 "일단 붙자"와 반대. 합격 확률보다 적합도가 중요한 층.)

**시장 근거 (리서치 확인):**
- 인크루트 2026 HR 조사(인사담당자 650명): "더 강화된 중고신입 선호"가 **이슈 1위(33.5%)**. AI 일자리 감소(21.5%)보다 높음.
- 기업 77%가 신입공채에 중고신입 지원받음 / 인사담당자 86.2% 중고신입 채용 의사.
- 채용 명시 인원: 2023년 246만 → 2024년 192만 → 2025년 154만 명(2년간 약 100만 감소).
- 신입 채용 축소·경력 선호는 일시적 흐름이 아니라 "구조적 변화"로 분석됨.

**검증 상태:**
- ✅ 확인됨: 중고신입·경력직 선호 시장이 크고 커지는 중 (위 데이터).
- ⏳ 미검증: 이 층이 "적합도 판단 도구"를 실제로 원하는가. → 목요일 유저 2명 피드백으로 확인.
- 구분: 시장 존재(데이터 확인) ≠ 제품 수요(검증 필요). 발표 시 이 선을 지킬 것.

---

## [2026-05] 확장 시 비용 구조 — 각자 API 키 발급 방향 (미래 검토)

- **결정:** 아직 결정 아님. 사용자가 늘 때를 대비한 방향 메모.
- **배경:** 현재 2명 테스트는 "내 키 공유 + 비밀번호" 방식. 편의 우선이라 소수까지만 유효. 사람이 늘면 (1) 내 비용 부담이 커지고 (2) 비번 유출 시 통제 불가 → 지속 불가능.
- **방향 후보:**
  - **A. 각자 키 발급** — 사용자가 본인 API 키 입력, 자기 쓴 만큼 자기 부담. 내 비용 0. (피드백에서 제안받은 방향.) 단점: 결제·발급 허들로 비개발자 이탈.
  - **B. 내가 전액 부담** — 사용자는 편하나 사람 늘수록 내 지갑 부담. 무료 배포 아니면 불가.
  - **C. 내가 호출 + 사용자 과금(SaaS)** — 구독·사용료로 API 비용 충당. 정석이나 결제·사용량측정·회원관리 필요. 제일 무거움.
- **단계 가이드:** 지금(2명)=내 키 공유 / 무료로 더 열 때=A(각자 키) / 제품화·수익화=C.
- **트리거:** 2명 검증이 "계속 쓰겠다"로 나오면 그때 A vs C 결정.

## [2026-05] 1인용 → 소규모 멀티유저로 전환

- **결정:** 본인 1인용에서, 몇 명이 함께 테스트하는 멀티유저 구조로 확장. 검증되면 더 넓게 열 수 있음.
- **이유:** MVP가 셀피쉬클럽에서 선정되어 발표함. "남들도 써보고 싶다"는 반응으로 수요 신호 확인. 한 번에 제품화하지 않고 소규모 테스트부터 단계적으로 연다.
- **방식:** API 키는 사용자가 각자 입력 (서비스가 비용 부담·결제 시스템 만들지 않음). 판단 기준 3개는 고정에서 사용자별 설정 가능한 템플릿으로. 데이터는 사용자별 분리.
- **주의:** 아래 "범용 → 1인용으로 좁힘" 결정을 되돌리는 것. 모순이 아니라 — 처음엔 빠른 완성을 위해 의도적으로 좁혔고, 검증된 뒤 다시 연 흐름이다.

## [2026-05] 범용 서비스 → 1인용으로 좁힘

- **결정:** 다른 사람도 쓸 수 있는 범용 서비스 대신 나만 쓰는 툴로.
- **이유:** 범용으로 만들려다 막혔음. API 공유 방법까지 고민했으나, 나만 쓰는 툴로 방향 잡으니 오히려 빠르게 완성됨. 고객이 한 명이면 판단 기준·기능·사용 이유가 전부 명확해진다.
- **교훈:** 가장 단순한 게 이긴다.

## [2026-05] 핵심 기준 체크 4개 → 3개 통합

- **결정:** 직무 적합 / 환경 적합 / 성장 적합 3개로 정리.
- **이유:** 기존 2번(브랜드 흐름을 볼 수 있는 포지션)과 4번(브랜드 흐름을 경험할 수 있는 환경)이 의미상 거의 동일. 실제로 쓰면서 중복을 발견해 통합·정교화. 기준이 적을수록 지원/패스 판단이 단순.
- **대안:** 4개 유지(도메인 적합성 분리). 도메인은 "직무 적합"에 흡수.

## [2026-05] 화장품 경력 필수 감지를 빼지 않고 살림

- **결정:** JD에 "화장품 경력 필수"가 있으면 자동 경고하는 기능 유지.
- **이유:** 화장품 경력이 없어서 처음엔 이 기능을 뺄까 고민. 하지만 지원할 수 없는 공고를 미리 걸러주는 = 시간을 아껴주는 기능이라고 판단. 거르는 것도 OS의 역할.
- **주의:** 1인용 시절 본인 맞춤 기능. 멀티유저로 가면서 산업 중립적으로 일반화하거나, 사용자가 켜고 끌 수 있는 옵션으로 전환 필요.

## [2026-05] FIT 점수 체계 도입

- **결정:** 단순 지원/보류/패스에 정량 FIT 점수 추가.
- **이유:** 판정만으로는 같은 "지원" 안에서 우선순위를 못 매김. 점수가 있으면 지원 순서가 생김.
- **주의:** 초기엔 점수가 전부 100으로 나와 변별력 부족 → 3개 기준 결과를 점수에 반영하도록 재설계 중.

## [2026-05] 2탭 → 3탭 구조 개편

- **결정:** 온보딩/JD분석기(2탭) → 프로필/공고판단/지원현황(3탭).
- **이유:** 분석 기록이 JD 분석기 탭에 섞여 복잡했음. 지원 현황을 독립 보드로 분리하니 흐름이 명확해짐.

## [2026-05] 다크모드 전환

- **결정:** 밝은 베이지 톤(#F8F5EC) → 다크모드.
- **이유:** 베이지에서 판정 배지(지원 추천/검토 필요/패스) 색 구분이 약했음. 어둡게 하니 초록/노랑/회색이 확실히 구분.

## [기획 초기] 판정이 패스여도 지원 가능하게

- **결정:** AI가 "패스" 판정해도 "그래도 지원할게요"로 진행 가능.
- **이유:** 핵심 철학 — 최종 결정은 사용자가 한다. 막으면 도구가 사용자 위에 서게 됨.

## [기획 초기] JD 이미지 업로드를 메인 입력으로

- **결정:** 텍스트 붙여넣기가 아니라 이미지 업로드를 기본으로.
- **이유:** 잡코리아 등이 텍스트 복사를 막아둠. 스크린샷을 Claude 비전으로 분석. 실사용 마찰 최소화.

---

## 핵심 인사이트 (3주차 미션 회고에서)

1. **Claude를 쓰는 게 아니라 작동하게 만드는 것** — 하네스 + 오케스트레이션. 쓰는 사람은 매번 프롬프트를 새로 짜고, 작동하게 만든 사람은 공고만 넣으면 된다.
2. **가장 단순한 게 이긴다** — 1인용으로 좁히니 더 강해졌다.
3. **엄두가 안 나는 걸 해결하는 게 OS의 역할** — 푸는 건 "공고 분석"이 아니라 "엄두가 안 나는 것". 생각할 게 없어지면 손이 간다.
